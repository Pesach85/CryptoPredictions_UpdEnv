# Lesson: Android must be on-device first

Remote FastAPI companion alone fails the native-APK quality gate. Port pure-OHLCV analytics (volatility radar, simple path compare) to Kotlin; keep heavy Python models optional remote.
